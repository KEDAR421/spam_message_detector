services:
  - type: web
    name: spam-detector
    env: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: NLTK_DATA
        value: /opt/render/project/src/.nltk_data
